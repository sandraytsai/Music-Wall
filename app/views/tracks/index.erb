<% @tracks.each do |track| %>
  <div class="track">
    <h3><a href="/tracks/<%= track.id %>"><%= track.title %></a><small>(by: <%= track.author %>)</small></h3>
    </div>
<% end %>

<hr/>

  <a class="btn-info btn" href="/tracks/new">Post a song!</a>

